#framework/layers.md

This document details each layer’s specific responsibility. It shows why the ordering exists and how information travels. It identifies tensions or harmonies between layers. It anchors the vertical structure of the system. It ensures each layer knows its boundaries.
